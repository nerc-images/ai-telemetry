apiVersion: route.openshift.io/v1
kind: Route
metadata:
  namespace: smartaquaculture
  name: aitelemetry
spec:
  host: aitelemetry.apps-crc.testing
  to:
    kind: Service
    name: aitelemetry
    weight: 100
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None
