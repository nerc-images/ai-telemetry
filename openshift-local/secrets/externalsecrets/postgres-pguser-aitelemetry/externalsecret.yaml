apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: postgres-pguser-aitelemetry
  namespace: smartaquaculture
spec:
  refreshInterval: 15s
  secretStoreRef:
    name: secret-store
    kind: SecretStore
  target:
    name: postgres-pguser-aitelemetry
  dataFrom:
    - extract:
        key: aitelemetry/kv/postgres-pguser-aitelemetry
